{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Patologia list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idpatologia</th>
                <th>Fecha</th>
                <th>Nombre</th>
                <th>Categoria</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('patologia_show', { 'id': entity.idPatologia }) }}">{{ entity.idPatologia }}</a></td>
                <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.categoria }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('patologia_show', { 'id': entity.idPatologia }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('patologia_edit', { 'id': entity.idPatologia }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('patologia_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
